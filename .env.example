# 환경변수 예시 파일
# 이 파일을 복사하여 .env로 사용하세요: cp .env.example .env

# 앱 설정
APP_ENV=development
APP_DEBUG=true

# OpenMC
OMP_NUM_THREADS=8

# 핵데이터 경로 (cross_sections.xml의 절대 경로)
# 미설정 시 nucdata/ 디렉토리에서 자동 탐색
# 핵데이터 설치: uv run python scripts/download_xs.py
OPENMC_CROSS_SECTIONS=nucdata/cross_sections.xml
